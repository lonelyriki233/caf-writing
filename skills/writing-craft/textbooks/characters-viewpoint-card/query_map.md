# Query Map

- If `characters exist without story job`, query `character-function`; apply gate: character_engine gate: relationship/function required.
- If `story promise unclear`, query `mice-quotient`; apply gate: compass gate: story mode named.
- If `reader investment weak`, query `sympathy-interest`; apply gate: draft gate: intro has investment hook.
- If `wrong POV drains tension`, query `viewpoint-selection`; apply gate: scene_card gate: POV justification.
- If `description replaces character`, query `character-through-choice`; apply gate: scene_card gate: choice under pressure.
- If `voices interchangeable`, query `attitude-and-voice`; apply gate: dialogue_map gate: voice card required.
- If `minor cast bloats`, query `minor-character-economy`; apply gate: revision gate: cast economy check.
- If `opening sells wrong story`, query `reader-promise`; apply gate: compass gate: promise matches execution.
