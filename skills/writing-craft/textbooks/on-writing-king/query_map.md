# Query Map

- If `description feels generic`, query `honest-observation`; apply gate: draft gate: description has story function.
- If `basic craft neglected`, query `toolbox`; apply gate: prose_audit gate: basic craft pass.
- If `plot feels forced`, query `situation-first`; apply gate: compass/story_engine gate: situation plus pressure.
- If `description over/underwritten`, query `description-selection`; apply gate: prose_audit gate: description proportion.
- If `dialogue too polished`, query `dialogue-honesty`; apply gate: dialogue_map gate: voice pressure.
- If `draft bloated`, query `second-draft-equals-first-minus`; apply gate: revision gate: compression pass.
- If `craft detached from reading`, query `read-write-discipline`; apply gate: revision gate: lesson captured.
- If `dialogue tags explain emotion`, query `avoid-adverbial-explaining`; apply gate: prose_audit gate: tag/adverb audit.
