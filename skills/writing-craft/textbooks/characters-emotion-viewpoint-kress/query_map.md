# Query Map

- If `character feels constructed`, query `credible-motivation`; apply gate: character_engine gate: motive and pressure.
- If `character introduction bland`, query `dominant-impression`; apply gate: draft gate: intro reveals function.
- If `emotion jumps too fast`, query `emotion-ladder`; apply gate: prose_audit gate: emotional progression.
- If `POV too distant accidentally`, query `viewpoint-depth`; apply gate: draft gate: POV distance named.
- If `too much/too little thought`, query `interiority-balance`; apply gate: draft gate: interiority serves choice.
- If `cast voices blur`, query `character-contrast`; apply gate: dialogue_map gate: voice card.
- If `feelings do not affect events`, query `emotion-and-plot`; apply gate: scene_card gate: emotion changes action.
- If `scene lacks focus`, query `viewpoint-revision`; apply gate: revision gate: POV function checked.
