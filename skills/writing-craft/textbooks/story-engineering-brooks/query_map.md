# Query Map

- If `idea is interesting but not story`, query `concept-premise-split`; apply gate: compass gate: concept + dramatic question required.
- If `drafting skips foundation`, query `six-core-competencies`; apply gate: revision gate: identify layer being repaired.
- If `plot has no architecture`, query `four-part-structure`; apply gate: volume_structure gate: act/part function required.
- If `inciting change too soft`, query `first-plot-point`; apply gate: volume_structure gate: plot point 1 forces commitment.
- If `middle does not transform`, query `midpoint-context-shift`; apply gate: volume_structure gate: midpoint escalation required.
- If `antagonistic pressure disappears`, query `pinch-points`; apply gate: volume_structure gate: recurring opposition signals.
- If `theme stays decorative`, query `theme-through-choice`; apply gate: theme_action gate: action proves theme.
- If `pretty prose before story works`, query `voice-after-architecture`; apply gate: draft gate: prose last.
