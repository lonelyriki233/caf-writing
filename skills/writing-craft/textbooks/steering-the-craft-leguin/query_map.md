# Query Map

- If `prose feels flat`, query `sentence-sound`; apply gate: prose_audit gate: rhythm pass required.
- If `sentences all same shape`, query `punctuation-rhythm`; apply gate: draft gate: rhythm varies with pressure.
- If `abstraction dominates`, query `concrete-language`; apply gate: prose_audit gate: abstract passages grounded.
- If `narration drifts`, query `pov-control`; apply gate: draft gate: POV consistency.
- If `explanation too direct`, query `indirect-exposition`; apply gate: prose_audit gate: info dramatized.
- If `scene pace wrong`, query `crowding-leaping`; apply gate: revision gate: pace mode intentional.
- If `voice generic`, query `voice-as-ear`; apply gate: prose_audit gate: sound check.
- If `style advice vague`, query `exercise-driven-revision`; apply gate: revision gate: exercise target named.
