# Query Map

- If `first scene lacks question`, query `hook-opening`; apply gate: chapter_spec gate: opening imbalance/question required.
- If `setup event and commitment blurred`, query `inciting-key-event`; apply gate: volume_structure gate: entry path identified.
- If `act I lingers`, query `first-plot-point-entry`; apply gate: volume_structure gate: no-return turn required.
- If `hero too proactive too soon or too passive too long`, query `first-half-reaction`; apply gate: volume_structure gate: response phase function.
- If `no central shift`, query `midpoint-reversal`; apply gate: volume_structure gate: midpoint changes agency.
- If `ending lacks darkness`, query `third-plot-point-low`; apply gate: volume_structure gate: all-is-lost pressure.
- If `climax is only spectacle`, query `climax-resolution`; apply gate: volume_structure gate: finale resolves dramatic question.
- If `scene chain is flat`, query `scene-disaster-sequel`; apply gate: scene_card gate: result launches next action.
