# Query Map

- If `language is default`, query `word-choice-conscious`; apply gate: prose_audit gate: key words earn place.
- If `sentences carry only plot`, query `sentence-attention`; apply gate: draft gate: sentence function.
- If `paragraphs uniform`, query `paragraph-motion`; apply gate: prose_audit gate: paragraph rhythm.
- If `emotion overexplained`, query `gesture-detail`; apply gate: dialogue_map/prose gate: unsaid embodied.
- If `dialogue polished but false`, query `dialogue-reading`; apply gate: dialogue_map gate: no Q&A dump.
- If `narrator voice vague`, query `narration-distance`; apply gate: draft gate: distance intentional.
- If `style copying risk`, query `learning-from-models`; apply gate: revision gate: anti-copy rule.
- If `revision too global`, query `slow-reading-revision`; apply gate: revision gate: paragraph-level audit.
