# Query Map

- If `prose rambles`, query `sentence-as-unit`; apply gate: prose_audit gate: sentence purpose clear.
- If `long sentences blur pressure`, query `short-sentence-pressure`; apply gate: draft gate: sentence length tied to pressure.
- If `prose padded`, query `remove-filler`; apply gate: revision gate: filler removed.
- If `description diffuse`, query `one-thing-seen`; apply gate: draft gate: concrete perception.
- If `editing only polishes grammar`, query `revision-by-reseeing`; apply gate: revision gate: function audit.
- If `attention unfocused`, query `reader-attention`; apply gate: prose_audit gate: attention flow.
- If `rules become mechanical`, query `against-formula`; apply gate: compass gate: rule purpose named.
- If `prose overexplains`, query `sound-and-silence`; apply gate: prose_audit gate: avoid overexplaining.
