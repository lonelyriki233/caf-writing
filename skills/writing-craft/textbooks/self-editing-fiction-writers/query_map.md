# Query Map

- If `telling replaces drama`, query `show-tell-control`; apply gate: revision gate: show/tell choice justified.
- If `dialogue tags/action clutter`, query `dialogue-mechanics`; apply gate: dialogue_map/prose gate: dialogue mechanics clean.
- If `minor things take too much space`, query `proportion`; apply gate: revision gate: proportion audit.
- If `thoughts explain too much`, query `interior-monologue`; apply gate: prose_audit gate: interiority nonredundant.
- If `prose states the obvious`, query `sophistication`; apply gate: prose_audit gate: no overexplained emotion.
- If `action beats are filler`, query `beats-as-business`; apply gate: dialogue_map gate: beat function.
- If `POV errors weaken immersion`, query `point-of-view`; apply gate: draft gate: POV knowledge consistent.
- If `revision unfocused`, query `layered-editing`; apply gate: revision gate: pass type declared.
